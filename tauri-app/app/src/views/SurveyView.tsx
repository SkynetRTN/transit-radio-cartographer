import { useState } from "react"; import { rpcClient } from "../ipc/client";
export function SurveyView({handle=1}:{handle?:number}){const [sel,setSel]=useState<{x0:number;x1:number}|null>(null);
return <section><h2>Survey</h2><div data-testid='survey-plot' onMouseDown={()=>setSel({x0:1,x1:2})} onDoubleClick={()=>sel&&rpcClient.request("cut_segment",{handle,start:sel.x0,end:sel.x1})} onContextMenu={(e)=>{e.preventDefault();setSel(null)}}>plot</div><button onClick={()=>rpcClient.smooth(handle)}>Smooth</button><button onClick={()=>rpcClient.baseline(handle)}>Baseline</button><button onClick={()=>rpcClient.align(handle,0.5)}>Align</button><button onClick={()=>rpcClient.makeImage(handle,1)}>Make Image</button><div data-testid='selection'>{sel?"selected":"none"}</div></section>}
